services:
  - type: web
    name: ai-call-backend
    env: node
    buildCommand: "npm install"
    startCommand: "npm start"
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 18